<!-- <h2>Display the picture</h2>
<%= button_to "Post", action: "index" %>
<h2>Share button here</h2>
<%= button_to "Share", action: "new" %>

<%= image_tag @photobooth.image.url(:medium) %> -->
<style>
body{
  background-image: url("https://cdn.pixabay.com/photo/2017/08/10/03/21/piano-2617645_960_720.jpg");
}
  #fa{
    text-align: center;
    color: lightgreen;
    font-size: 2em;
  }
  h1{
    text-align: center;
  }
  .button {
    background-color: #339999;
    border: none;
    border-radius: 45%;
    color: white;
    padding: 15px 33px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 10px 10px;
    cursor: pointer;
}
p{
  font-size: 2em;
  text-align: center;
}
</style>
<body>

<p id="fa"><i class="fa fa-check-circle fa-5x" aria-hidden="true"></i>
</p>
<h1>DONE!</h1>
<!-- add a picture beteween the donr and the music genre title -->

<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
<div class="container">
  <div class="panel-group">
    <div class="panel panel-primary">
      <div class="panel-heading"><p>Select A Genre</p></div>

      <%= link_to 'Latin', new_music_genre_path, class: 'button' %>
      <%= link_to 'Rock', new_music_genre_path, class: 'button' %>
      <%= link_to 'Country', new_music_genre_path, class: 'button' %>
      <%= link_to 'Blues', new_music_genre_path, class: 'button' %>
      <%= link_to 'Pop', new_music_genre_path, class: 'button' %>
      <%= link_to 'HipHop', new_music_genre_path, class: 'button' %>
    </div>
  </div>
</div>
    <% @music_genres.each do |music_genre| %>
      <tr>
        <td><%= music_genre.name %></td>
        <td><%= link_to 'Show', music_genre %></td>
        <td><%= link_to 'Edit', edit_music_genre_path(music_genre) %></td>
        <td><%= link_to 'Destroy', music_genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</body>
