<h1>HI, I AM YOUR USER PROFILE PAGE</h1> <br>

<%= form_for(@document) do |f| %>
	  <div class="field">
	    <%= f.file_field :file %>
	  </div>
	  <div class="actions">
	    <%= f.submit %>
	  </div>
<% end %>

<%= link_to 'Log Out', destroy_user_session_path %>
